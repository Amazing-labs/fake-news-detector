# Prisma Database URLs
# For Prisma Client (Session Pooler) - Port 6543
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:6543/postgres?pgbouncer=true"

# For Prisma Migrations (Direct Connection) - Port 5432
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres"

# Better Auth
# BETTER_AUTH_URL is the PUBLIC url the browser reaches auth on, not the
# worker's own hostname. In production the browser talks to the Vercel origin
# and Vercel rewrites /api/* to the worker, so this must be the Vercel URL:
#   BETTER_AUTH_URL="https://<your-app>.vercel.app/api/auth"
# TRUSTED_ORIGINS must likewise contain the origin the browser sends (Vercel
# forwards it), comma-separated; localhost entries are always merged in.
#   BETTER_AUTH_TRUSTED_ORIGINS="https://<your-app>.vercel.app"
BETTER_AUTH_SECRET="replace-with-a-32-plus-character-secret"
BETTER_AUTH_URL="http://localhost:3000/api/auth"
BETTER_AUTH_TRUSTED_ORIGINS="http://localhost:3000,http://localhost:5173"

# Set to "production" on the deployed worker: it is what switches Better Auth
# to secure cookies.
NODE_ENV="development"

# Supabase Storage (server-side media deletion)
# The server deletes bucket objects with the service-role key, which bypasses
# RLS. Both vars are required for deletion to work. Never expose the
# service-role key to the client and never prefix it with VITE_.
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
# Optional: override the storage bucket name (defaults to "fake-news-media").
# SUPABASE_STORAGE_BUCKET="fake-news-media"

# Storage reconciliation sweep (scheduled cron only)
# Arms the daily orphan sweep to actually delete. When unset or not "true", the
# cron runs in dry-run and only logs what it would delete. The manual scripts
# (sweep:apply / sweep:force) ignore this — they are driven by their CLI flags.
STORAGE_SWEEP_ENABLED="false"
